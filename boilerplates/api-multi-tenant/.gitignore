# Build artifacts del boilerplate api-multi-tenant.
target/
fitz.lock

# Binarios sueltos que `fitz build` deja adyacentes al main.
src/main
src/main.exe
src/main.pdb

# Variables de entorno con secrets reales.
.env
