[package]
name = "fitz-api-fullstack-postgres"
version = "0.1.0"
edition = "2026"
description = "Fullstack boilerplate — CRUD completo de tasks con Fitz + SQLAlchemy + Postgres + frontend HTML+JS vanilla"

[bin]
main = "src/main.fitz"
